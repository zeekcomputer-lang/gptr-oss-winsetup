# venv / vendored repo
.venv/
vendor/

# env / secrets
.env

# setup 이 생성하는 파생 requirements (원본 vendor 미수정)
.gptr-build-requirements.txt

# outputs / logs
outputs/
*.log

# 로컬 데이터(사용자 코퍼스/변환본) — git 에 올리지 않음. examples/ 는 추적함.
data/

# python
__pycache__/
*.pyc

# models cache
.cache/
models/

# 오프라인 리소스 번들(setup 이 다운로드) — tiktoken 블록 / NLTK 데이터. 재현 가능하므로 추적 제외.
offline/
